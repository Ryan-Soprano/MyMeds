import { StyleSheet } from "react-native";

// This is the default style from App.js to test in Expo
// Replace with real styling
const Style = StyleSheet.create({
    container:{
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    }
});